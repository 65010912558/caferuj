# ใช้ PHP 8.2 ที่มี Apache มาให้
FROM php:8.2-apache

# เปิดใช้งาน mod_rewrite ถ้าจำเป็น (ใช้กับบางเว็บเช่น Laravel)
RUN a2enmod rewrite

# ตั้ง working directory ภายใน container
WORKDIR /var/www/html

# คัดลอกไฟล์ทั้งหมดจากโฟลเดอร์ปัจจุบันไปที่ container
COPY . /var/www/html

# ถ้าคุณมีไฟล์ index.php ใน subdirectory เช่น `carousel/sign-in/`
# คัดลอกไฟล์นี้ไปยัง container (ถ้าต้องการ)
COPY ./carousel/sign-in/index.php /var/www/html/index.php

# สามารถเพิ่มการตั้งค่าอื่น ๆ ตามที่ต้องการ
